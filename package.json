{
  "name": "sample-nwjs",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "cd src && npm install && npm run build && cd .. && nwbuild --mode=build --glob=false --out-dir=dist src/",
    "start": "lib\\nwjs-v0.78.1-win-x64\\nw.exe src\\"
  },
  "author": "",
  "license": "BSD-2-Clause",
  "devDependencies": {
    "nw-builder": "4.3.7"
  }
}
